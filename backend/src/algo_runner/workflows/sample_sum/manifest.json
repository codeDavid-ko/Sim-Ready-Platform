{
  "id": "sample-sum",
  "hidden": true,
  "name": "샘플 — 1..n 합",
  "description": "스타터 기본 예시. n을 받아 1..n 합 + 업로드 파일 정보를 반환 (레지스트리에 워크플로우가 2개 이상 끼워짐을 보이는 데모).",
  "icon": "🧮",
  "version": "1.0.0",
  "requiredPermissions": [],
  "entry": "sample-sum",
  "io": {
    "input": {
      "params": { "n": "integer" },
      "file": { "label": "파일 (선택)", "required": false }
    },
    "output": ["report"]
  }
}
