# Sim-Ready Platform — 추가 카드 아이디어 (딥리서치, 2026-06-12)

> 목적: 지금 카드(형상→USD 변환, 재질추론, 물성추론, 관절, SD 텍스처, 턴테이블 렌더, NVIDIA content-agents 비교, 자동화 파이프라인)를 **빼고**, 추가하면 가치 있을 "단일 기능 카드" 후보 정리.
> 방법: 5각도 웹검색 → 26개 소스(대부분 NVIDIA 1차 문서) → 118개 주장 추출 → 3표 적대검증(24 확정 / 1 기각). **아직 적용 안 함 — 검토용.**

---

## 0. 한눈에 — 우선순위

| 순위 | 카드 후보 | 가치 | 난이도 | 우리 자산 재사용 | 근거 |
|---|---|---|---|---|---|
| ★1 | **콜리전 메시 생성 (Convex Decomposition / VHACD)** | 매우 높음(물리 필수 관문) | 낮음~중 | pxr/PhysX, 기존 USD 입력 파이프 | 확정 3-0 |
| ★2 | **USD 검증 (Asset Validator · SimReady 룰셋 · 자동수정)** | 높음(품질 게이트) | 낮음~중 | usdchecker 이미 일부 사용 중 | 확정 3-0 |
| 3 | **Replicator 합성데이터 생성(SDG) — bbox·세그멘테이션 라벨** | 높음(지각모델 학습용) | 중~높음 | Isaac 렌더 인프라 | 확정 3-0 |
| 4 | **시맨틱 라벨링 / 씬 그래프** | 중(3번의 전제조건) | 낮음 | Claude 추론으로 자동 라벨 | 확정 3-0 |
| 5 | **URDF/MJCF 로봇 임포트 → USD articulation** | 높음(인접 영역) | 중~높음 | 관절 카드 로직 | 확정 3-0 |
| 6 | **도메인 랜덤화 (재질·스케일·색)** | 중 | 낮음~중 | 재질·텍스처 카드 | 확정 3-0 |
| 7 | **USD Asset Structure 3.0 재구성/패키징** | 중(유지보수) | 낮음~중 | 전 카드 공통 규약 | 확정 3-0 |
| (미검증) | 메시 경량화(decimation/LOD) · CAD 어셈블리 구조 보존 · 물리 드롭테스트 검증 · 그랩 생성 · 변전소/GIS 특화 | — | — | — | 근거 미확보(아래 §9) |

> **가장 추천: ★1 콜리전 메시 + ★2 USD 검증.** 둘 다 sim-ready의 "필수 통과 관문"이고, API가 안정적이며, 기존 물성추론·렌더 카드와 겹치지 않고 보완한다.

---

## 1. ★ 콜리전 메시 생성 (Convex Decomposition / VHACD)

- **무엇:** 시각 메시(오목·복잡 형상)를 물리 충돌용으로 **여러 개의 볼록(convex) 덩어리로 분해**해 붙인다. 헐(hull) 수가 적으면 빠르고, 많으면 정확. `physics:approximation` 토큰 + `PhysicsCollisionAPI`/`PhysicsMeshCollisionAPI` 를 **원본 위에 'over' 오버레이**로 얹는다(형상은 안 건드림).
- **왜 sim-ready 필수:** Isaac/PhysX에서 **오목 메시는 그대로 충돌체로 못 씀** → convex decomposition이 표준. 이게 없으면 물체가 서로 뚫고 들어가거나(interpenetration) 시뮬이 깨진다. **sim-ready의 가장 기본 물리 요구사항.**
- **기존 도구/표준:** `PhysxSchema.PhysxConvexDecompositionCollisionAPI`(VoxelResolution ~5만~500만), Isaac Lab v2.3.0이 `convert_mesh.py`에 Collision Mesh Schema 속성 추가. URDF Importer도 4가지 충돌옵션(Convex Hull / Convex Decomposition / Bounding Sphere / Cube) 제공.
- **난이도:** 낮음~중. 우리는 이미 USD를 열고 API를 얹는다(관절·물성 카드). 같은 패턴으로 충돌 API + voxel 해상도 슬라이더만 추가하면 됨. 결과를 턴테이블로 시각확인 가능.
- **플랫폼 적합:** 물성추론 카드(질량/마찰)와 **짝** — "질량+마찰+충돌형상"이 다 있어야 진짜 sim-ready. 파이프라인에 `형상정리 → 충돌메시 → 물성` 으로 끼우기 좋음.
- 출처: simulation_fundamentals(Isaac), IsaacLab #2400, IsaacLab release_notes, asset_structure tutorial(6.0). ⚠ "MeshConverter 기본 voxel이 너무 낮아 충돌이 과대"는 **기각(0-3)** — 기본값 나쁘다고 가정 금지.

## 2. ★ USD 검증 (Asset Validator · SimReady 룰셋 · 자동수정)

- **무엇:** USD/USDZ를 규칙셋에 맞춰 검사. `usdchecker`(OpenUSD CLI)는 상호운용·Hydra 렌더 가능성 검사(읽기전용). **NVIDIA Omniverse Asset Validator**는 그 확장 — GUI/CLI/Python API, 커스텀 규칙, 그리고 **`--fix` 자동수정**까지. **전용 "SimReady" 룰셋** + 물리 규칙(rigid body·collider·articulation·mass)을 내장 → sim-ready 컴플라이언스를 **기계적으로 체크/수정** 가능.
- **왜 sim-ready 필수:** "이 USD가 Isaac에서 제대로 돌아갈까?"를 **출력 전에 자동 판정**하는 품질 게이트. 우리 카드들이 만든 USD가 규약을 지키는지 검증 + 자동수정 → 신뢰성↑.
- **기존 도구:** `pip install omniverse-asset-validator`, usd-exchange-samples의 assetValidator CLI, usdchecker(우리 mass-physics에 `validate_usd`로 이미 일부 사용 중).
- **난이도:** 낮음~중(CLI/Python API 존재). 업로드 USD → 검증 리포트(통과/위반 + 자동수정본 다운로드).
- **플랫폼 적합:** 모든 USD 산출 카드의 **후처리/QA 단계**로 파이프라인 끝에 붙이기 좋음. 우리 자체 sim-ready 규약(Z-up·meters·충돌·mass·재질바인딩) 커스텀 룰도 추가 가능.
- 출처: learn-openusd asset-validation, kit asset-validator, usd-exchange-samples.

## 3. Replicator 합성데이터 생성 (SDG) — bbox·세그멘테이션 라벨

- **무엇:** Omniverse Replicator(`omni.replicator`)로 **라벨 달린 합성 이미지 데이터셋** 생성. 내장 annotator(시맨틱/인스턴스 세그멘테이션, 2D tight/loose bbox, 3D bbox) + writer(BasicWriter, KITTI/Tao) → RGB + KITTI 포맷 객체검출 라벨.
- **왜 중요:** 지각(perception) 모델 학습용 데이터를 **수작업 라벨링 없이** 대량 생성. sim-ready 자산의 핵심 활용처(로봇 비전·검사 AI 학습).
- **기존 도구:** Isaac Sim Replicator(annotators + writers).
- **난이도:** 중~높음(카메라/씬/writer 설정 필요). 우리 Isaac 렌더 인프라 재사용 가능하나 SDG 파이프라인은 별도 셋업.
- **플랫폼 적합:** 턴테이블 렌더 카드의 "발전형". #4 시맨틱 라벨링이 선행돼야 함.
- 출처: replicator_overview, getting-started SDG, writer_control.

## 4. 시맨틱 라벨링 / 씬 그래프 (#3의 전제)

- **무엇:** 씬 prim에 **객체 클래스(시맨틱)** 부여 — Semantics Schema Editor(Type=class, Data=label) 또는 코드(`rep.modify.semantics([('class','door')])`). 세그멘테이션·bbox를 뽑으려면 **먼저 시맨틱이 있어야** 함.
- **왜 중요:** 모든 라벨 출력의 하드 전제조건. 저비용으로 SDG 카드를 열어준다.
- **우리 강점:** **Claude로 부품명→클래스 자동 라벨**(우리가 이미 재질/물성에 부품 추론 중) → "AI 시맨틱 자동 태깅" 카드로 차별화 가능.
- **난이도:** 낮음.
- 출처: semantics_schema_editor, getting-started SDG.

## 5. URDF/MJCF 로봇 임포트 → USD articulation

- **무엇:** Isaac Sim 6.0 URDF Importer가 URDF→USD articulation(UsdPhysics + PhysX/Newton 조인트 스키마 자동 + ArticulationRootAPI), 충돌옵션 4종. MJCF(MuJoCo XML) 임포터도 존재. Isaac Lab은 URDF→USD 배치 변환(v2.1.1) + MJCF 스포너(v2.3.2) + Mesh 변환기(OBJ/FBX/STL/glTF, mass·충돌·convexDecomposition).
- **왜 중요:** 로봇 자산(URDF/MJCF)을 우리 플랫폼에서 USD articulation으로 받아들이는 입구. 관절 카드와 인접.
- **난이도:** 중~높음(임포터 연동). 단, Isaac에 임포터가 이미 있으니 호출 오케스트레이션 위주.
- **플랫폼 적합:** 관절 카드의 fixed-base 로직과 시너지. ⚠ URDF 임포터 충돌옵션이 ★1과 겹침 — 통합 검토.
- 출처: urdf importer(6.0), mjcf importer(5.0), IsaacLab release_notes, import_new_asset.

## 6. 도메인 랜덤화 (재질·스케일·색)

- **무엇:** Isaac Lab v2.1.0의 **USD-레벨 랜덤화** + texture/scale/color 이벤트 텀(시뮬 전 USD 속성에 작용) → sim-ready USD에 직접 도메인 랜덤화.
- **왜 중요:** sim-to-real 갭 줄이는 표준 기법. 학습 데이터 다양성↑.
- **난이도:** 낮음~중(자산이 이미 제대로 된 USD 구조면). SD 텍스처·재질 카드와 결합 가능.
- 출처: IsaacLab release_notes(PR #2040, #2121).

## 7. USD Asset Structure 3.0 재구성 / 패키징 (가로지르는 규약)

- **무엇:** Isaac Sim 6.0 권장 — 자산을 **관심사별 파일 분리**(geometry/materials/instances/robot metadata/base/physics/physx/root interface), 충돌은 오버레이 API 컴포지션.
- **왜 중요:** 리뷰/유지보수/다운스트림 호환성. 단독 기능이라기보다 **전 카드 공통 출력 규약**으로 적용하는 게 더 가치.
- **난이도:** 낮음~중. "Asset Structure 3.0으로 재구성" 카드 또는 패키징 컨벤션.
- 출처: asset_structure tutorial(6.0).

---

## 8. 추천 묶음 (파이프라인 관점)

- **"완전 sim-ready" 체인:** `형상정리 → 충돌메시(★1) → 물성추론(기존) → 재질추론(기존) → USD검증(★2)` → 검증 통과한 완결 자산.
- **"학습데이터" 체인:** `sim-ready 자산 → 시맨틱 자동태깅(#4) → 도메인 랜덤화(#6) → Replicator SDG(#3)` → 라벨 데이터셋.
- 즉 **★1·★2를 먼저** 넣으면 기존 카드들과 합쳐 "진짜 시뮬 들어가는 자산"이 완성되고, 그 위에 #4·#6·#3로 "데이터 생산"까지 확장.

---

## 9. 미검증 / 추가 조사 필요 (정직히 표기)

아래는 질문 범위였으나 **이번 리서치에서 검증된 근거를 못 찾음** — 부정이 아니라 "근거 미확보". 적용 전 별도 조사 필요:

- **메시 경량화(decimation/LOD):** Isaac/Omniverse 1차 도구·충돌메시와의 상호작용 불명확. (Simplygon 등 외부 도구 존재 — 소스에 등장하나 미검증)
- **CAD 어셈블리 구조 보존(STEP/JT 계층·조인트·kinematics → USD articulation):** 어떤 도구가 어셈블리 트리를 보존하는지 미확인. (NVIDIA "CAD-to-USD" 블로그 소스 있음)
- **물리 드롭테스트/안정성 검증(정착·관통·관성 안정):** 표준 Isaac 워크플로우인지, 커스텀 구축인지 불명확. (우리가 시뮬 돌려 자체 구축 가능성)
- **그랩 생성·로봇 조작(GraspGen, grasp SDG):** NVlabs/GraspGen, Isaac grasping SDG 소스는 있으나 검증 주장 미생존.
- **변전소/GIS(IEC 61850) 디지털트윈 특화:** Exelon 드론검사 SDG 블로그, aif-pipeline-samples, dsx 소스 등장하나 검증 미생존. KEPCO 맥락에선 별도 딥리서치 권장.

---

## 출처 (1차 NVIDIA/OpenUSD 문서 위주)
- Isaac simulation_fundamentals · asset_structure(6.0) · urdf/mjcf importer · replicator overview/writer · semantics_schema_editor
- learn-openusd asset-validation · kit asset-validator · usd-exchange-samples assetValidator
- IsaacLab release_notes · import_new_asset · #2400 · discussions
- (참고/미검증) CAD-to-USD 블로그 · Simplygon · NVlabs/GraspGen · grasping SDG · Exelon SDG · aif-pipeline-samples · dsx

> 검증 통계: 6각도, 26소스, 118주장 추출, 25 검증 → **24 확정 / 1 기각**. 대부분 3-0 만장일치, 일부 2-1(버전별 API 이동 등 — 배포 버전에 맞춰 API명 재확인 권장).
